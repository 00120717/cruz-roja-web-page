import { GetterTree } from 'vuex';
import { RootState } from '@/store/types';
import { PacienteState } from '@/store/modules/paciente/types';

export const getters: GetterTree<PacienteState, RootState> = {
};

export default getters;
