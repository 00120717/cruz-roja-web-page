import { GetterTree } from 'vuex';
import { RootState } from '@/store/types';
import { VoluntarioState } from '@/store/modules/voluntario/types';

export const getters: GetterTree<VoluntarioState, RootState> = {
};

export default getters;
