import { GetterTree } from 'vuex';
import { RootState } from '@/store/types';
import { PermisoState } from '@/store/modules/permiso/types';

export const getters: GetterTree<PermisoState, RootState> = {
};

export default getters;
