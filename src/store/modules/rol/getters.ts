import { GetterTree } from 'vuex';
import { RootState } from '@/store/types';
import { RolState } from '@/store/modules/rol/types';

export const getters: GetterTree<RolState, RootState> = {
};

export default getters;
