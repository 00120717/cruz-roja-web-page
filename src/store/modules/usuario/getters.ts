import { GetterTree } from 'vuex';
import { UsuarioState } from '@/store/modules/usuario/types';
import { RootState } from '@/store/types';

export const getters: GetterTree<UsuarioState, RootState> = {
};

export default getters;
